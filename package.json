{
  "name": "warp-m3tering-contract",
  "version": "1.0.0",
  "description": "Implements a warp contract for the M3tering Protocol",
  "author": "Switch Electric",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "clean": "rimraf bundle",
    "build": "rimraf bundle && node tools/build.js",
    "deploy": "rimraf bundle && node tools/build.js && node tools/deploy.js",
    "redeploy": "rimraf bundle && node tools/build.js && node tools/redeploy.js",
    "format": "prettier --write 'contract/*.ts'",
    "interact": "node tools/interact.js",
    "test": "node tests/test.js"
  },
  "dependencies": {
    "m3tering-ed25519": "^1.0.1",
    "m3tering-ethers": "^1.0.1",
    "warp-contracts": "^1.4.33",
    "warp-contracts-plugin-deploy": "^1.0.13"
  },
  "devDependencies": {
    "esbuild": "*",
    "prettier": "*",
    "replace-in-file": "*",
    "rimraf": "*"
  }
}